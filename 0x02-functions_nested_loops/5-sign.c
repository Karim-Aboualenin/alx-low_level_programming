#include "main.h"

/**
 * print_sign - indicate the sign of a number
 * @n: number to check
 * Return: 1 if c is postive, 0 if zero and -1 if negative
 */
int print_sign(int n)
{
if (n > 0)
return (1);
if (n == 0)
return (0);
return (-1);
}
